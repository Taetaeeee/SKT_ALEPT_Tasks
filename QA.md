# T01 점검 기록

## 공개 범위
- 공개: 관심 분야, 문제 해결 방식, 프로젝트 경험, 취미
- 비공개: 전화번호, 개인 이메일, 상세 주소, 학교명/개인 식별 정보, 비밀번호·토큰·API 키

## 소개 근거 3개
1. 자동화
   - 상황: 유튜브 댓글을 팀에서 수작업으로 수집
   - 행동: Python 댓글 수집 프로그램 제작 및 공유
   - 결과: 반복 수집 작업을 프로그램 실행 방식으로 전환
2. 데이터 구조 설계
   - 상황: 프린트 토너 신청 자동화 팀 프로젝트
   - 행동: 요청→서버→DB→화면 흐름 기준으로 SQL 구조 설계
   - 결과: 프론트엔드·백엔드가 같은 데이터 구조로 연동
3. 원인 분석
   - 상황: Hyperledger Fabric TLS 통신 오류
   - 행동: 컨테이너/Peer/Orderer 로그, 인증서 경로·권한 점검
   - 결과: 인증서 경로·권한 수정 후 Peer 통신 복구

## 초기 구현/검사에서 발견해 수정한 실제 결함
1. 수정 전: 소개 문장은 있었지만 ‘누구에게 무엇을 보여 주는 페이지인지’ 대상과 목적이 한 문장으로 명확하지 않았음.
   수정 후: “이 페이지는 IT 인프라·네트워크·보안 분야에 관심 있는 사람에게…” 문장으로 대상·목적을 명시함.
2. 수정 전: 상단 ‘취향’ 링크가 기본적으로 숨겨진 영역 내부(`#likes`)를 가리켜, 영역이 닫힌 상태에서는 링크를 눌러도 보이는 변화가 없었음.
   수정 후: 작동하지 않는 상단 링크를 제거하고, ‘좋아하는 것 보기’ 버튼 하나로 상호작용 경로를 명확히 함.
3. 수정 전: 버튼 검사에서 `aria-expanded`는 `true`로 바뀌지만 `hidden` 값 계산이 반대로 되어 취향 영역이 실제로 열리지 않았음.
   수정 후: `likesPanel.hidden = isOpen`으로 수정해 마우스·Enter·Space에서 열고 닫히도록 고침.
4. 수정 전: 하단 공개 범위 문구에는 관심 분야·프로젝트·취미만 적혀 있었지만 실제 페이지에는 ‘문제 해결 방식’도 공개되어 점검표와 화면이 불일치했음.
   수정 후: 공개 범위 문구에 ‘문제 해결 방식’을 추가해 실제 공개 내용과 맞춤.
5. 수정 전: 고정 헤더가 있는데 내부 앵커에 `scroll-margin-top`이 없어 내부 링크 이동 시 섹션 상단이 헤더에 가려질 수 있었음.
   수정 후: 주요 앵커에 `scroll-margin-top: 76px`을 적용함.

## 접근성/구조 정적 점검
- 제목: h1 → h2 → h3 순서로 건너뛰지 않음
- 상호작용: native `<button>` 사용, `aria-expanded`/`aria-controls` 제공
- 키보드: 링크와 버튼은 기본 Tab 이동 가능, 버튼은 Enter/Space 실행 가능
- 초점: `:focus-visible` 외곽선 제공
- 움직임: 큰 애니메이션 없음. `prefers-reduced-motion: reduce`도 반영
- 대비: 주요 글자/배경 조합은 계산상 4.5:1 이상
- 비밀값: 소스에 비밀번호·토큰·API 키를 넣지 않음

## 현재 검사 결과
- 1366×768: 소개·활동·근거가 첫 화면에 보임 → 통과
- 1920×1080: 소개·활동·근거가 첫 화면에 보임 → 통과
- 가로 넘침: 두 해상도 모두 0px → 통과
- 콘솔 빨간 오류: 테스트 환경에서 0건 → 통과
- Tab 순서: 본문 바로가기 → 브랜드 → 소개 → 경험 → 경험 근거 보기 → 좋아하는 것 보기 → 통과
- 마우스 클릭: 취향 영역 열기/닫기 → 통과
- 키보드 Enter/Space: 취향 영역 열기/닫기 → 통과

## 공개 배포 후 마지막으로 확인할 항목
- 새 시크릿 창에서 결과물 URL과 소스 URL이 무로그인으로 열리는지
- 공개 배포 주소에서 DevTools Console 빨간 오류가 0건인지 재확인
- Git 기록을 포함해 비밀번호·토큰·API 키 원문이 0건인지 재확인
